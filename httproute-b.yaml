apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: serverb-route
  namespace: tenant-b
spec:
  parentRefs:
  - name: shared-gateway
    namespace: default
  hostnames:
  - serverb.example.com
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: serverb-service
      port: 5000
      namespace: tenant-b
